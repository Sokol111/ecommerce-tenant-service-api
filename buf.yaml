version: v2

modules:
  - path: proto/rpc
    name: buf.build/sokol111/tenant-api
  - path: proto/events

deps:
  - buf.build/googleapis/googleapis

lint:
  use:
    - DEFAULT

breaking:
  use:
    - FILE
